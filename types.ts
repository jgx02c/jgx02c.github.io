export interface Section {
    id: number;
    titleLogo: string;  // You can specify this as string or adjust it as necessary based on the actual type
    subText: string;
    description: string;
    linkText: string;
    link: string;
    image: string;  // Adjust the type based on the actual image type
}
